{
  "name": "chris-stirling-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "@astrojs/sitemap": "latest",
    "@wix/blog": "latest",
    "@wix/sdk": "latest",
    "@wix/stores": "latest",
    "astro": "latest"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
