# Wix Headless OAuth client ID (public). Copy this file to .env
PUBLIC_WIX_CLIENT_ID=bc0192c3-415c-4080-a60a-b7282bc2878d

# YouTube Data API key — build-time only (NOT public). Set in Cloudflare env vars.
# Used to fetch the latest long-form video for the homepage embed.
YOUTUBE_API_KEY=
